# Phase 1 (data pipeline) needs only the Python standard library.
# The packages below are for Phase 2 (training).
#
# Colab notes:
#  * torch AND numpy are preinstalled and tied together — we do NOT pin either.
#    Pinning them downgrades numpy and breaks Colab's torch (ABI mismatch).
#  * We use loose floors and ride Colab's environment. train.py guards against
#    the TRL/transformers API differences across versions (tokenizer vs
#    processing_class, evaluation_strategy vs eval_strategy), so this is safe.
#  * gradio is intentionally absent — not needed for training; install it
#    separately only for the Phase 3 chat UI.
transformers>=4.45
trl>=0.11
peft>=0.13
bitsandbytes>=0.44
accelerate>=0.34
datasets>=2.20
pyyaml>=6.0
